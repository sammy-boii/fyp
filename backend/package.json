{
  "name": "backend",
  "version": "0.1.0",
  "scripts": {
    "dev": "npx kill-port 5000 && bun run --hot src/index.ts",
    "prisma:generate": "bun prisma generate --schema ../shared/src/prisma/schema.prisma",
    "build": " bun run prisma:generate && bun build src/index.ts --outdir dist --target bun",
    "start": "bun dist/index.js"
  },
  "dependencies": {
    "@hono/oauth-providers": "^0.8.5",
    "@huggingface/inference": "^4.13.11",
    "@types/pdfkit": "^0.17.4",
    "arctic": "^3.7.0",
    "csv-parse": "^6.1.0",
    "discord.js": "^14.25.1",
    "hono": "^4.9.7",
    "jsonwebtoken": "^9.0.2",
    "mammoth": "^1.11.0",
    "pdf-parse-fork": "^1.2.0",
    "pdfkit": "^0.17.2",
    "shared": "workspace:*",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
